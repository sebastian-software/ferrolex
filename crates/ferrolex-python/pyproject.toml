[build-system]
requires = ["maturin>=1.9.4,<2"]
build-backend = "maturin"

[project]
name = "ferrolex-python"
version = "0.1.0"
description = "Experimental Python binding spike for ferrolex"
requires-python = ">=3.9"

[tool.maturin]
module-name = "ferrolex_python"
bindings = "pyo3"
